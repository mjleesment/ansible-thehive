---
-docker_prereq_packages:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg-agent
  - python3-pip

-docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-compose

-docker_pip_modules:
  - docker
  - PyYAML
  - docker-compose
